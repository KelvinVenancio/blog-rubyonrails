<div id="page_wrapper">
	<h1>Novo Post</h1>

	<%= form_for :post, url: posts_path do |f| %>
		<p>
			<%= f.label :título %><br>
			<%= f.text_field :title %>
		</p>

		<p>
			<%= f.label :mensagem %><br>
			<%= f.text_area :body %>
		</p>

		<p>
			<%= f.submit %>
		</p>
	<% end %>
</div>