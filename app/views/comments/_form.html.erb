<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :Nome %><br>
		<%= f.text_field :name %>
	</p>
	
	<p>
		<%= f.label :Comentário %><br>
		<%= f.text_area :body %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
<% end %>